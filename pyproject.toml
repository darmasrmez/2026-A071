[project]
name = "2026-a071"
version = "0.1.0"
description = "Measuring carbon footprint of LLM fine-tuning"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
]
